name: Publish to OpenClaw Hub

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g., 0.2.0)'
        required: true

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install OpenClaw Hub CLI
        run: npm install -g openclaw

      - name: Determine version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            # Strip 'v' prefix if present
            VERSION="${{ github.event.release.tag_name }}"
            VERSION="${VERSION#v}"
          else
            VERSION="${{ github.event.inputs.version }}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Publishing version: $VERSION"

      - name: Publish to OpenClaw Hub
        env:
          OPENCLAW_TOKEN: ${{ secrets.OPENCLAW_TOKEN }}
        run: |
          openclaw login --token "$OPENCLAW_TOKEN" --no-browser
          openclaw publish . \
            --slug read-no-evil-mcp \
            --name "read-no-evil-mcp" \
            --version "${{ steps.version.outputs.version }}" \
            --changelog "${{ github.event.release.body || 'Release' }} (requires read-no-evil-mcp>=${{ steps.version.outputs.version }})"
